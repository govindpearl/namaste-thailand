import 'package:flutter/material.dart';

class AppInPurchaseScreen extends StatelessWidget {
  const AppInPurchaseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Center(
        child: Text("AppInPurchase"),
      )),
    );
  }
}
